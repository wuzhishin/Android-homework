<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.hitsz" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AircraftWar2023" >
        <activity
            android:name="edu.hitsz.activity.GameActivity"
            android:exported="false" />
        <activity android:name="edu.hitsz.activity.SQLiteWriteActivity" />
        <activity android:name="edu.hitsz.activity.RankActivity" />
        <activity android:name=".activity.RegisterActivity"/>
        <service android:name=".music.MusicService"
            android:enabled="true"
            android:exported="true"/>
        <service android:name=".music.SoundPoolService"
            android:enabled="true"
            android:exported="true"/>
        <activity android:name=".activity.LoginActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity

            android:name="edu.hitsz.activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">

        </activity>
    </application>

</manifest>